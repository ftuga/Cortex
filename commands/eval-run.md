---
description: Run LongMemEval on stored reflexions — measure P@1, P@k, MRR
allowed-tools: Bash(bash:*)
---

Generate query variants from stored reflexions and measure retrieval quality against published benchmarks (OMEGA 95.4%, Mastra 94.9%, Zep 71.2%).

Run:
```bash
bash ~/.claude/helpers/longmemeval.sh run
```
