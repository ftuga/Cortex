# cline adapter

**status:** planned for v1.1.

cline has workflows and rule files. mapping:

- reflexion memory → same CLI, cline-independent
- ERL → `routing-heuristics.md` loaded as a cline rule
- SPEAK → `skills/speak/SKILL.md` installed as a cline rule
- routing enforcement → cline workflow that runs on task spawn

planned for v1.1. contributions welcome with `adapter: cline` tag.
